// Used to cause `parseFiles` to encounter a loop / ignore 2nd visit.
import * as dir2 from '../dir2';

/**
 * A test of directory importing `index.js`.
 */
export const dir1Test: string = 'A test of directory import';
